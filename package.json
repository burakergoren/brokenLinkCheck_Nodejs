{
  "name": "broken-link-check",
  "version": "1.0.0",
  "description": "broken link check on a single html web page",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "burakergoren",
  "license": "ISC",
  "dependencies": {
    "broken-link-checker": "^0.7.8"
  }
}
